{
  "name": "@negokaz/visio-mcp-server",
  "version": "0.1.0",
  "description": "An MCP server that reads and writes diagram data to MS Visio files",
  "author": "negokaz",
  "license": "MIT",
  "bin": {
    "visio-mcp-server": "dist/launcher.js"
  },
  "scripts": {
    "build": "goreleaser build --snapshot --clean && tsc",
    "watch": "tsc --watch",
    "debug": "npx @modelcontextprotocol/inspector dist/launcher.js"
  },
  "devDependencies": {
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
